VITE_API_URL=http://localhost:4000/api

# Activa el mock API en memoria (sin backend)
# VITE_DEV_NOAUTH=1

# Junto con VITE_DEV_NOAUTH, salta el login y entra como Dilan Test (Admin)
# Útil para desarrollo rápido. Si lo quitas, verás el flujo de login real (con cortinas).
# VITE_DEV_AUTOLOGIN=1
